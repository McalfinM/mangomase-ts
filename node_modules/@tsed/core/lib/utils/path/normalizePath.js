"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizePath = void 0;
const path_1 = require("path");
const isArray_1 = require("../objects/isArray");
const isString_1 = require("../objects/isString");
const fixPath = require("normalize-path");
function normalizePath(item, ...paths) {
    if (isString_1.isString(item)) {
        const path = path_1.join(item, ...paths);
        return fixPath(path);
    }
    if (isArray_1.isArray(item)) {
        return item.map((item) => normalizePath(item));
    }
    return item;
}
exports.normalizePath = normalizePath;
//# sourceMappingURL=normalizePath.js.map